resource: mcn_secret_policy_rule
workspace: multi-cloud-network-connect
route: /manage/secrets/secret_policy_rules
namespace_scoped: false
add_action: none
discovered_at: "2026-06-17"
form_pattern: list-only
note: >
  List-only page — no "Add" tab or create form. Secret policy rules are
  created inline within Secret Policy objects (via the Rules configure-link
  in secret_policy). Same pattern as service_policy_rule and
  bigip_virtual_server.

summary:
  form_pattern: list-only
  note: "No standalone create form. Rules created within parent Secret Policy resource."
